{
  "openai_api_key": "\"\"\" CoreX - Core Extractor Interfaccia Web Streamlit con vista compatta espandibile \"\"\"  import streamlit as st import os import sys import json import shutil import subprocess import platform from pathlib import Path from datetime import datetime  # Setup path sys.path.insert(0, str(Path(__file__).parent))  # Directory base BASE_DIR = Path(__file__).parent SYLLABUS_DIR = BASE_DIR / \"data\" / \"syllabus\" FRAMEWORKS_DIR = BASE_DIR / \"data\" / \"frameworks\" OUTPUT_DIR = BASE_DIR / \"data\" / \"outputs\" SETTINGS_FILE = BASE_DIR / \".settings.json\"  # Crea directory se non esistono for d in [SYLLABUS_DIR, FRAMEWORKS_DIR, OUTPUT_DIR]:     d.mkdir(parents=True, exist_ok=True)  # ============================================================ # FUNZIONI DI UTILITY # ============================================================  def load_settings():     if SETTINGS_FILE.exists():         with open(SETTINGS_FILE, 'r') as f:             return json.load(f)     return {}  def save_settings(settings):     with open(SETTINGS_FILE, 'w') as f:         json.dump(settings, f, indent=2)  def open_folder(path):     \"\"\"Apre cartella nel file manager - Windows\"\"\"     path = Path(path)     path.mkdir(parents=True, exist_ok=True)          if platform.system() == \"Windows\":         os.startfile(str(path))     elif platform.system() == \"Darwin\":         subprocess.run([\"open\", str(path)])     else:         subprocess.run([\"xdg-open\", str(path)])  def get_materie():     \"\"\"Ottiene lista materie (cartelle di primo livello)\"\"\"     if not SYLLABUS_DIR.exists():         return []          materie = []     for item in sorted(SYLLABUS_DIR.iterdir()):         if item.is_dir() and not item.name.startswith('.'):             materie.append(item.name)     return materie  def get_sottocartelle(materia):     \"\"\"Ottiene sottocartelle (classi di laurea) di una materia\"\"\"     materia_path = SYLLABUS_DIR / materia     if not materia_path.exists():         return []          sottocartelle = []     for item in sorted(materia_path.iterdir()):         if item.is_dir() and not item.name.startswith('.'):             sottocartelle.append(item.name)     return sottocartelle  def get_pdf_files(materia, sottocartella=None):     \"\"\"Ottiene file PDF in una cartella\"\"\"     if sottocartella:         folder = SYLLABUS_DIR / materia / sottocartella     else:         folder = SYLLABUS_DIR / materia          if not folder.exists():         return []          return sorted([f.name for f in folder.glob(\"*.pdf\")])  def count_pdfs(materia):     \"\"\"Conta tutti i PDF in una materia (incluse sottocartelle)\"\"\"     materia_path = SYLLABUS_DIR / materia     if not materia_path.exists():         return 0          count = len(list(materia_path.glob(\"*.pdf\")))     for sub in materia_path.iterdir():         if sub.is_dir():             count += len(list(sub.glob(\"*.pdf\")))     return count  # ============================================================ # INTERFACCIA STREAMLIT # ============================================================  st.set_page_config(     page_title=\"CoreX - Core Extractor\",     page_icon=\"\ud83e\uddec\",     layout=\"wide\" )  # Carica settings settings = load_settings()  # Titolo st.title(\"\ud83e\uddec CoreX - Core Extractor\") st.caption(\"Estrazione Framework Disciplinari da Programmi Universitari\")  # ============================================================ # SIDEBAR - CONFIGURAZIONE # ============================================================ with st.sidebar:     st.header(\"\u2699\ufe0f Configurazione\")          # API Key     saved_key = settings.get(\"openai_api_key\", \"\")     api_key = st.text_input(         \"OpenAI API Key\",         value=saved_key,         type=\"password\",         help=\"Inserisci la tua API key OpenAI\"     )          save_key = st.checkbox(\"Ricorda API Key\", value=bool(saved_key))          if save_key and api_key:         if api_key != saved_key:             settings[\"openai_api_key\"] = api_key             save_settings(settings)     elif not save_key and saved_key:         settings.pop(\"openai_api_key\", None)         save_settings(settings)          if api_key:         os.environ[\"OPENAI_API_KEY\"] = api_key          st.divider()          # Opzioni elaborazione     use_llm = st.checkbox(\"Usa LLM per labeling\", value=bool(api_key))     n_clusters = st.slider(\"Numero moduli (0=auto)\", 0, 25, 0)          st.divider()     st.caption(\"CoreX v1.0 - Zanichelli\")  # ============================================================ # GESTIONE CARTELLE - VISTA COMPATTA # ============================================================  st.header(\"\ud83d\udcc1 Raccolta Programmi\")  # Inizializza stato sessione if 'materia_espansa' not in st.session_state:     st.session_state.materia_espansa = None if 'sottocartella_selezionata' not in st.session_state:     st.session_state.sottocartella_selezionata = None  # Azioni globali col1, col2, col3 = st.columns([2, 2, 2])  with col1:     nuova_materia = st.text_input(\"Nuova materia:\", placeholder=\"es. Chimica_Organica\")  with col2:     st.write(\"\")  # Spacer     st.write(\"\")     if st.button(\"\u2795 Crea Materia\", disabled=not nuova_materia):         nome = nuova_materia.strip().replace(\" \", \"_\")         if nome:             (SYLLABUS_DIR / nome).mkdir(exist_ok=True)             st.success(f\"Creata: {nome}\")             st.rerun()  with col3:     st.write(\"\")     st.write(\"\")     if st.button(\"\ud83d\udd04 Aggiorna Lista\"):         st.rerun()  st.divider()  # Lista materie compatta materie = get_materie()  if not materie:     st.info(\"\ud83d\udced Nessuna materia trovata. Crea una cartella con il pulsante sopra.\") else:     for materia in materie:         n_pdf = count_pdfs(materia)         sottocartelle = get_sottocartelle(materia)         is_espansa = (st.session_state.materia_espansa == materia)                  # Riga compatta per materia         col1, col2, col3, col4, col5 = st.columns([0.5, 3, 1, 1, 1])                  with col1:             # Icona espandi/comprimi             icon = \"\ud83d\udcc2\" if is_espansa else \"\ud83d\udcc1\"             if st.button(icon, key=f\"toggle_{materia}\"):                 if is_espansa:                     st.session_state.materia_espansa = None                     st.session_state.sottocartella_selezionata = None                 else:                     st.session_state.materia_espansa = materia                     st.session_state.sottocartella_selezionata = None                 st.rerun()                  with col2:             # Nome materia             label = f\"**{materia.replace('_', ' ')}**\"             if sottocartelle:                 label += f\" ({len(sottocartelle)} classi)\"             if n_pdf > 0:                 label += f\" - {n_pdf} PDF\"             st.markdown(label)                  with col3:             if st.button(\"\ud83d\udcc2\", key=f\"open_{materia}\", help=\"Apri cartella\"):                 open_folder(SYLLABUS_DIR / materia)                  with col4:             if st.button(\"\u270f\ufe0f\", key=f\"rename_{materia}\", help=\"Rinomina\"):                 st.session_state[f\"renaming_{materia}\"] = True                 st.rerun()                  with col5:             if st.button(\"\ud83d\uddd1\ufe0f\", key=f\"delete_{materia}\", help=\"Elimina\"):                 st.session_state[f\"confirm_delete_{materia}\"] = True                 st.rerun()                  # Form rinomina         if st.session_state.get(f\"renaming_{materia}\"):             with st.form(key=f\"rename_form_{materia}\"):                 nuovo_nome = st.text_input(\"Nuovo nome:\", value=materia)                 col_a, col_b = st.columns(2)                 with col_a:                     if st.form_submit_button(\"\u2713 Conferma\"):                         nuovo_nome = nuovo_nome.strip().replace(\" \", \"_\")                         if nuovo_nome and nuovo_nome != materia:                             old_path = SYLLABUS_DIR / materia                             new_path = SYLLABUS_DIR / nuovo_nome                             if not new_path.exists():                                 old_path.rename(new_path)                                 st.session_state.materia_espansa = None                         st.session_state[f\"renaming_{materia}\"] = False                         st.rerun()                 with col_b:                     if st.form_submit_button(\"\u2717 Annulla\"):                         st.session_state[f\"renaming_{materia}\"] = False                         st.rerun()                  # Conferma eliminazione         if st.session_state.get(f\"confirm_delete_{materia}\"):             st.warning(f\"\u26a0\ufe0f Eliminare '{materia}' e tutti i suoi contenuti?\")             col_a, col_b = st.columns(2)             with col_a:                 if st.button(\"\u2713 S\u00ec, elimina\", key=f\"confirm_yes_{materia}\"):                     shutil.rmtree(SYLLABUS_DIR / materia)                     st.session_state.materia_espansa = None                     st.session_state[f\"confirm_delete_{materia}\"] = False                     st.rerun()             with col_b:                 if st.button(\"\u2717 Annulla\", key=f\"confirm_no_{materia}\"):                     st.session_state[f\"confirm_delete_{materia}\"] = False                     st.rerun()                  # Contenuto espanso         if is_espansa:             with st.container():                 st.markdown(\"---\")                                  # Sottocartelle (classi di laurea)                 if sottocartelle:                     st.markdown(\"**Classi di laurea:**\")                                          # Aggiungi nuova classe                     col1, col2 = st.columns([3, 1])                     with col1:                         nuova_classe = st.text_input(                             \"Nuova classe:\",                             placeholder=\"es. L-13\",                             key=f\"new_class_{materia}\"                         )                     with col2:                         st.write(\"\")                         st.write(\"\")                         if st.button(\"\u2795\", key=f\"add_class_{materia}\"):                             if nuova_classe:                                 (SYLLABUS_DIR / materia / nuova_classe.strip()).mkdir(exist_ok=True)                                 st.rerun()                                          # Lista sottocartelle                     for sub in sottocartelle:                         sub_path = SYLLABUS_DIR / materia / sub                         n_sub_pdf = len(list(sub_path.glob(\"*.pdf\")))                         is_selected = (st.session_state.sottocartella_selezionata == sub)                                                  col1, col2, col3, col4 = st.columns([0.5, 3, 1, 1])                                                  with col1:                             icon = \"\ud83d\udcc2\" if is_selected else \"\ud83d\udcc1\"                             if st.button(icon, key=f\"sub_toggle_{materia}_{sub}\"):                                 if is_selected:                                     st.session_state.sottocartella_selezionata = None                                 else:                                     st.session_state.sottocartella_selezionata = sub                                 st.rerun()                                                  with col2:                             st.markdown(f\"`{sub}` - {n_sub_pdf} PDF\")                                                  with col3:                             if st.button(\"\ud83d\udcc2\", key=f\"open_sub_{materia}_{sub}\"):                                 open_folder(sub_path)                                                  with col4:                             if st.button(\"\ud83d\uddd1\ufe0f\", key=f\"del_sub_{materia}_{sub}\"):                                 shutil.rmtree(sub_path)                                 st.session_state.sottocartella_selezionata = None                                 st.rerun()                                                  # Mostra file PDF nella sottocartella                         if is_selected:                             pdfs = get_pdf_files(materia, sub)                             if pdfs:                                 st.markdown(\"**File PDF:**\")                                 for pdf in pdfs:                                     col1, col2 = st.columns([4, 1])                                     with col1:                                         st.text(f\"  \ud83d\udcc4 {pdf}\")                                     with col2:                                         if st.button(\"\ud83d\uddd1\ufe0f\", key=f\"del_pdf_{materia}_{sub}_{pdf}\"):                                             (sub_path / pdf).unlink()                                             st.rerun()                             else:                                 st.caption(\"Nessun PDF. Usa il pulsante \ud83d\udcc2 per aprire la cartella e salvare i file.\")                                  else:                     # Nessuna sottocartella - mostra file direttamente nella materia                     st.markdown(\"**Opzioni:**\")                                          col1, col2 = st.columns(2)                     with col1:                         nuova_classe = st.text_input(                             \"Crea classe di laurea:\",                             placeholder=\"es. L-13\",                             key=f\"new_class_empty_{materia}\"                         )                         if st.button(\"\u2795 Crea\", key=f\"create_class_{materia}\"):                             if nuova_classe:                                 (SYLLABUS_DIR / materia / nuova_classe.strip()).mkdir(exist_ok=True)                                 st.rerun()                                          # File PDF direttamente nella cartella materia                     pdfs = get_pdf_files(materia)                     if pdfs:                         st.markdown(\"**File PDF nella cartella:**\")                         for pdf in pdfs:                             col1, col2 = st.columns([4, 1])                             with col1:                                 st.text(f\"  \ud83d\udcc4 {pdf}\")                             with col2:                                 if st.button(\"\ud83d\uddd1\ufe0f\", key=f\"del_pdf_root_{materia}_{pdf}\"):                                     (SYLLABUS_DIR / materia / pdf).unlink()                                     st.rerun()                     else:                         st.caption(\"\ud83d\udced Cartella vuota. Crea sottocartelle per classe o salva PDF direttamente qui.\")                                  st.markdown(\"---\")  # ============================================================ # ELABORAZIONE # ============================================================  st.header(\"\ud83d\udd2c Elaborazione\")  # Selezione materia e classe per elaborazione materia_sel = st.selectbox(     \"Seleziona materia da elaborare:\",     options=[\"\"] + materie,     index=0 )  if materia_sel:     sottocartelle = get_sottocartelle(materia_sel)          if sottocartelle:         classe_sel = st.selectbox(             \"Seleziona classe di laurea:\",             options=[\"Tutte le classi\"] + sottocartelle         )     else:         classe_sel = None          # Raccogli tutti i PDF da elaborare     pdf_files = []     if classe_sel and classe_sel != \"Tutte le classi\":         folder = SYLLABUS_DIR / materia_sel / classe_sel         pdf_files = list(folder.glob(\"*.pdf\"))     else:         folder = SYLLABUS_DIR / materia_sel         # PDF nella cartella principale         pdf_files = list(folder.glob(\"*.pdf\"))         # PDF nelle sottocartelle         for sub in sottocartelle:             pdf_files.extend(list((folder / sub).glob(\"*.pdf\")))          st.info(f\"\ud83d\udcca {len(pdf_files)} programmi trovati\")          # Framework esistente opzionale     existing_fw = st.file_uploader(         \"Framework esistente (opzionale):\",         type=[\"json\"],         help=\"Carica un framework esistente per il confronto\"     )          # Nome framework     fw_name = st.text_input(         \"Nome framework:\",         value=f\"Framework Empirico {materia_sel.replace('_', ' ')}\"     )          # Pulsante elaborazione     if st.button(\"\ud83d\ude80 Avvia Elaborazione\", type=\"primary\", disabled=len(pdf_files) == 0):         if len(pdf_files) == 0:             st.error(\"Nessun PDF da elaborare!\")         else:             # Salva framework esistente se caricato             existing_path = None             if existing_fw:                 existing_path = FRAMEWORKS_DIR / \"temp_existing.json\"                 with open(existing_path, \"wb\") as f:                     f.write(existing_fw.read())                          # Crea cartella temporanea con i PDF selezionati             temp_pdf_dir = BASE_DIR / \"data\" / \"temp_processing\"             temp_pdf_dir.mkdir(exist_ok=True)                          # Copia PDF nella cartella temporanea             for pdf in pdf_files:                 shutil.copy(pdf, temp_pdf_dir / pdf.name)                          try:                 from app.main_pipeline import run_pipeline                                  progress = st.progress(0)                 status = st.empty()                                  status.text(\"Estrazione testo da PDF...\")                 progress.progress(20)                                  results = run_pipeline(                     pdf_dir=str(temp_pdf_dir),                     existing_framework=str(existing_path) if existing_path else None,                     output_dir=str(OUTPUT_DIR),                     framework_name=fw_name,                     use_llm=use_llm,                     n_clusters=n_clusters if n_clusters > 0 else None                 )                                  progress.progress(100)                 status.text(\"Completato!\")                                  st.success(f\"\u2705 Elaborazione completata! File salvati in: {results['output_dir']}\")                                  # Mostra risultati                 st.subheader(\"\ud83d\udcca Risultati\")                                  for file in results.get('files', []):                     file_path = OUTPUT_DIR / file                     if file_path.exists():                         with open(file_path, 'r', encoding='utf-8') as f:                             content = f.read()                                                  with st.expander(f\"\ud83d\udcc4 {file}\"):                             if file.endswith('.json'):                                 st.json(json.loads(content))                             else:                                 st.text(content)                                                  st.download_button(                             f\"\u2b07\ufe0f Scarica {file}\",                             content,                             file_name=file,                             mime=\"application/json\" if file.endswith('.json') else \"text/plain\"                         )                              except Exception as e:                 st.error(f\"\u274c Errore: {e}\")                 import traceback                 st.code(traceback.format_exc())                          finally:                 # Pulizia                 if temp_pdf_dir.exists():                     shutil.rmtree(temp_pdf_dir)                 if existing_path and existing_path.exists():                     existing_path.unlink()  else:     st.info(\"\ud83d\udc46 Seleziona una materia per iniziare l'elaborazione\")  # Footer st.divider() st.caption(\"CoreX - Estrazione Framework Disciplinari | Zanichelli\")"
}